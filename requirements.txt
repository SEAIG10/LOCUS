# LOCUS consolidated Python dependencies
# Install what you need per subsystem (FL server, realtime sensors, training, etc.)

# --- Core runtime (Federated server/client + ZeroMQ bridges) ---
numpy>=1.26.0
paho-mqtt>=1.6.1
pyzmq>=25.0.0
python-dateutil>=2.8.2
tensorflow>=2.13.0
flwr>=1.8.0

# --- Vision & audio sensors / data prep (optional) ---
ultralytics>=8.0.0      # pulls torch as a dependency for YOLO
opencv-python>=4.8.0
sounddevice>=0.4.6
librosa>=0.10.0
matplotlib>=3.7.0

# --- Utilities / developer tooling ---
tqdm>=4.65.0
pytest>=7.4.0
